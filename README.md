# Pdef

Small browser PDF text editor, to edit existing text in a pdf file.

## Local development

```sh
npm install
npm run dev
```

## Static build

```sh
npm run build
```

The static site is written to `dist/`.
